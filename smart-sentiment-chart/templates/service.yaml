apiVersion: v1
kind: Service
metadata:
  name: {{ include "smart-sentiment-chart.fullname" . }}
  labels:
    {{- include "smart-sentiment-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smart-sentiment-chart.selectorLabels" . | nindent 4 }}
